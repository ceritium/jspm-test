# Test project to integrate jspm and karma

```
$ npm install
$ jspm install

# To see the site
$ live-server

# Run tests
$ karma start # Run with Chrome and Firefox

$ karma start --browsers Chrome
$ karma start --browsers Firefox
